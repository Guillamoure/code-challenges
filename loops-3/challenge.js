// Create a function that accepts an array of numbers
// The array will list numbers that should indicate their index
// For example, the array should look like this [0, 1, 2, 3, 4, 5, 6, 7, 8]

// If the array has an element that does not match its index
// Return an array of all of the incorrect indexes
// Otherwise, return an empty array

// For example, given the array [0, 1, 56]
// Return [2]

// Given the array [0, 1, 2, 34, 56]
// Return [3, 4]

function indexChecker(array){
	// Write code here
}

// To test code, type the below command into your terminal
// npm test -- loops-3


// Ignore this line
module.exports = indexChecker
